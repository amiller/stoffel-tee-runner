[package]
name = "stoffel-lobby"
version = "0.1.0"
edition = "2021"
license = "Apache-2.0"

[dependencies]
lobby-records = { path = "../lobby-records" }
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
hex = "0.4"
ed25519-dalek = { version = "2", features = ["std"] }
